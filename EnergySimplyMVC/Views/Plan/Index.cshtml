@model IEnumerable<EnergySimplyDAL.Models.Plan>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlanName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlanFixedCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PlanVarCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlanName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlanFixedCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PlanVarCost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PlanID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PlanID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlanID })
        </td>
    </tr>
}

</table>
